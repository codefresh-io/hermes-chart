apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "hermes.name" . }}-store-backup
  labels:
    app: {{ template "hermes.name" . }}-store-backup
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.backup.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ template "hermes.name" . }}-store-backup
    spec:
      affinity:
{{ toYaml ( default .Values.global.affinity .Values.backup.affinity ) | indent 8 }}
      containers:
      - env:
        - name: APP_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              key: env
              name: app
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: redis-password
              name: {{ template "hermes.name" . }}-store
        - name: S3_URL
          valueFrom:
            secretKeyRef:
              key: s3-url
              name: {{ template "hermes.name" . }}-store-backup
        - name: SLACK_CHANNEL
          valueFrom:
            secretKeyRef:
              key: slack-channel
              name: {{ template "hermes.name" . }}-store-backup
        - name: SLACK_WEBHOOK_URL
          valueFrom:
            secretKeyRef:
              key: slack-url
              name: {{ template "hermes.name" . }}-store-backup
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              key: aws-access-key-id
              name: {{ template "hermes.name" . }}-store-backup
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              key: aws-secret-access-key
              name: {{ template "hermes.name" . }}-store-backup
        image: {{ .Values.backup.image }}:{{ .Values.backup.tag }}
        imagePullPolicy: Always
        name: {{ .Chart.Name }}-store-backup
      imagePullSecrets:
      - name: docker-registry
      restartPolicy: Always

